Здравствуйте.

В приложении находится архив условия лабораторной, запакованный 7z с
четырёхсимвольным паролем, состоящим из цифр от 0 до 9.
Внутри этого архива находится tar архив, в котором имеется файл
tex2html-test/testme.sh.

Требуется в своём репозитории создать директорию cryptosed ровно с
двумя файлами:
* uncrypt.sh -- скрипт, подбирающий пароль к 7z-архиву, объёмом менее
50 строк кода (где каждая команда bash на своей строке).
* и ещё каким-то файлом, таким, что если его поместить в директорию
tex2html-test, полученную из tar архива, и запустить testme.sh,
последний напечатает "Accepted" (проверять это я, понятное дело, буду
на своей машине).

Сами архивы и их производные, получаемые при распаковке, в репозиторий
класть не нужно.

В скриптах можно использовать только встроенные возможности bash, grep и sed.

Hint:
cat << EOF
<html>
<head>
</head>
<body>
EOF
cat << EOF
</body>
</html>
EOF

